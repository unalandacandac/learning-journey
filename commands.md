# Linux Ders 3 — Temel Komutlar

## 📍 Konum ve gezinme

`pwd` → Nerede olduğumu gösterir.

`ls` → Bulunduğum dizindeki dosya ve klasörleri gösterir.

`ls -l` → Dosya ve klasörleri ayrıntılı gösterir.

`cd klasör` → Belirtilen klasöre girer.

`cd ..` → Bir üst klasöre çıkar.

`cd /home/backdoor-future` → Belirtilen konuma doğrudan gider.

`cd ~` → Home dizinine gider.

`.` → Bulunduğum dizini ifade eder.

`..` → Bir üst dizini ifade eder.

`~` → Kullanıcının Home dizinini ifade eder.

---

## 📁 Dosya ve klasör

`touch dosya.txt` → Boş dosya oluşturur.

`mkdir klasör` → Yeni klasör oluşturur.

`rm dosya.txt` → Dosya siler.

---

## 🔀 Git temel akışı

`git status` → Git'in mevcut durumunu gösterir.

`git add .` → Bulunduğum yerdeki değişiklikleri Git'e ekler.

`git commit -m "mesaj"` → Değişiklikleri Git geçmişine kaydeder.

`git push` → Commit'leri GitHub'a gönderir.

---

## 🧠 Ezber sırası

pwd → Neredeyim?

ls → Ne var?

cd → Gir.

cd .. → Geri çık.

touch → Dosya oluştur.

mkdir → Klasör oluştur.

rm → Sil.

git status → Git ne durumda?

git add . → Git'e ekle.

git commit → Kaydet.

git push → GitHub'a gönder.

---

## 🎯 Ders 3 Ana Mantık

Linux:

Konum → Gör → Gir → Oluştur → Sil

Git:

Kontrol et → Ekle → Commit et → GitHub'a gönder
